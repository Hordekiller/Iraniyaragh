NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
# The real staff-auth HTTP client against /api/v1/auth/* is the default.
# NEXT_PUBLIC_FIXTURE_AUTH=true substitutes the deterministic fixture for local
# dev / e2e builds only; any other value (or absence) uses the live backend.
NEXT_PUBLIC_FIXTURE_AUTH=false
