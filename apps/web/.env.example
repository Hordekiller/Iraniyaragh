# Example auth fixture opt-in for the storefront (docs/AUTH_CONTRACT.md §17/§18).
#
# Copy to `.env.development` for local dev, or commit a `.env.fixture-e2e` for
# the Playwright build. NEVER set this in a real production deployment:
# production must use a real AuthHttpClient (no fixture).
# VITE_FIXTURE_AUTH=true