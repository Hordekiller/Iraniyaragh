NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
# Opt-in fixture auth for local dev / e2e builds only. Any other value
# (or absence) stays fail-closed so shipped builds can never demo-auth.
NEXT_PUBLIC_FIXTURE_AUTH=false
