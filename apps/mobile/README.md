# Iraniyaragh Mobile

Customer mobile application boundary. Planned stack: React Native + Expo + TypeScript, consuming the same `/api/v1` contracts as web and admin. Implementation starts after authentication, catalog, inventory availability, cart and order contracts stabilize.
