# E2E build mode: allow the pre-backend AUTH + CATALOG fixtures so the smoke
# suite can exercise the storefront before the real backend lands
# (AUTH_CONTRACT §17; User UI lane of AGENT_WORKSTREAMS.md).
VITE_FIXTURE_AUTH=true
VITE_FIXTURE_CATALOG=true