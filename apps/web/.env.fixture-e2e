# E2E build mode: allow the explicit in-memory AUTH, CATALOG and COMMERCE
# adapters. Production builds never enable these flags; the purchase test also
# stops at PENDING_PAYMENT and never simulates a successful gateway callback.
VITE_FIXTURE_AUTH=true
VITE_FIXTURE_CATALOG=true
